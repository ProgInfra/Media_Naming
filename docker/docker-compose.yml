---
name: media-naming

services:
  media-naming:
    container_name: media-naming
    image: progower/media-naming:1.2.0
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    restart: "no"
